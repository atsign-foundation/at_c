name: Unit Tests
on:
  push:
    branches: [trunk]
  pull_request:
    branches: [trunk]

jobs:
  unit_tests:
    strategy:
      matrix:
        os: [macos-latest, ubuntu-latest, windows-latest]
        package: [atchops, atclient]
    runs-on: ${{ matrix.os }}
    steps:
      - name: CMake Configure, Make Install, and CTest
        uses: threeal/cmake-action@v1.3.0
        with:
          source-dir: packages/${{ matrix.package }}
          generator: Unix Makefiles
          args: -D$(echo "${{ matrix.package }}" | tr '[:lower:]' '[:upper:]')_BUILD_TESTS=ON
          build-dir: packages/${{ matrix.package }}/build
          run-build: true
          build-args: --target install
          run-test: true
          test-args: -V --output-on-failure

